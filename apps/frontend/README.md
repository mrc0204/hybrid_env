# Frontend

Not yet scaffolded. Built in **Milestone 4** per the project roadmap:
React + TypeScript + Vite + TailwindCSS + Zustand + React Query.

The frontend is presentation only — dashboard, environment map, AI chat,
notifications, timeline, recommendation cards, user settings. No business
logic; it consumes the Backend API and `@ai-env/contracts` types.

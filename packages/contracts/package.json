{
  "name": "@ai-env/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared communication contracts (events, API envelope) between Frontend, Backend, and AI Core",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
